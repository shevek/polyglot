#macro(implement $object)
#foreach ($interface in $helper.getAnnotations($object, "javaImplements"))
        , $interface
#end
#end
##
#macro(annotate $object)
#foreach ($annotation in $helper.getAnnotations($object, "javaAnnotation"))
    $annotation
#end
#end